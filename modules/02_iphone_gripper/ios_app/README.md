# iOS App Directory

Current active app:

```text
UMIFTiPhoneCaptureCore/
```

Archived apps and experiments:

```text
legacy/
```

Shared iOS dependencies:

```text
vendor/
```

New iPhone-side work should go into `UMIFTiPhoneCaptureCore`.
