# UMIFT DataCollect 源码交付说明

交付日期：2026-08-12

本压缩包是 `<PATH_TO_DELIVERABLES>` 在交付日期的当前源码快照，包含四路数据采集、笔记本端对齐与 UMI-FT Zarr 导出主链路，以及运行所需的项目文档、格式定义、CoinFT 标定模型和第三方依赖。

## 1. 目录内容

```text
UMIFT_datacollect_source_20260812/
├── 交付说明.md
├── README.md
├── DIRECTORY_MAP.md
├── modules/
│   ├── 01_global_camera/           # RealSense D435/D435i 全局 RGB/Depth
│   ├── 02_iphone_gripper/          # iPhone ARKit、深度、位姿、夹爪开合度
│   ├── 03_coinft_teensy/           # 双路 CoinFT、Teensy 固件和标定模型
│   └── 04_laptop_alignment_export/ # Mac 三路协调、录制、对齐和 Zarr 导出
├── docs/                            # Markdown 项目、架构和操作文档及配图
├── tools/                           # 对齐、导出等辅助工具
├── schemas/                         # 数据格式定义
└── reference/                       # 项目内引用的参考说明与小型资产
```

推荐先阅读：

1. `交付说明.md`（本文件）
2. `README.md`
3. `modules/04_laptop_alignment_export/docs/01_operator_guide.md`
4. 对应设备模块的 `README.md`

## 2. 交付范围与排除项

本包保留源码、测试、Xcode 工程、Teensy 固件、CoinFT production ONNX/norm 模型、iOS OpenCV framework、librealsense 源码与已安装运行库。

为避免把本机数据和可再生成内容混入源码交付，未包含：

- `runs/` 真实采集数据（原项目约 247 GB）
- `deliverables/` 内的历史交付包和备份
- `show/` 演示文稿和生成材料
- 录制文件、captures、运行日志和本地 artifacts
- Xcode DerivedData、Python 缓存、pytest 缓存
- librealsense 编译中间目录和嵌套 Git 元数据
- 重复的 `opencv-4.9.0-ios-framework.zip`；已保留解压后的 `opencv2.framework`
- PDF、DOCX、PPTX 等非源码文档；相关 Markdown 正文和配图已保留

本包不是采集数据备份。需要复现实验数据时，应单独交付相应 `runs/<run_id>/`。

## 3. 环境与硬件

当前主链路面向 macOS，主要需要：

- macOS、完整 Xcode 和 Xcode Command Line Tools
- Python 3.9 环境
- Python 包：`av`、`cv2`、`numpy`、`pyserial`、`zarr`；测试还需 `pytest`
- iOS 17 或更高版本的 iPhone，开启开发者模式并信任 Mac
- Intel RealSense D435/D435i
- Teensy 4.1 和两路 CoinFT

注意：项目目前没有统一的根级 `requirements.txt` 或 `environment.yml`，全新电脑上的 Python 环境尚不能一键重建。原开发机使用的已验证解释器为：

```text
<conda-env>/bin/python
```

文档中的 `<PATH_TO_DELIVERABLES>`、Xcode 路径、串口名均是原开发机示例；解压到其他位置或换设备后必须按实际路径调整。

## 4. 解压后的基本检查

```bash
cd UMIFT_datacollect_source_20260812
export UMIFT_ROOT="$PWD"

test -f modules/04_laptop_alignment_export/entrypoints/receiver_web_gui.py \
  && echo "project files: ok"

python3 -c 'import av, cv2, numpy, serial, zarr; print("python dependencies: ok")'
```

## 5. 四个模块的主要入口

### 5.1 RealSense 全局相机

```bash
bash modules/01_global_camera/run_capture_macos.sh --help-wrapper
bash modules/01_global_camera/run_capture_macos.sh list
bash modules/01_global_camera/run_capture_macos.sh test
```

详细说明：`modules/01_global_camera/README.md`。

### 5.2 iPhone 采集 App

Xcode 工程：

```text
modules/02_iphone_gripper/ios_app/UMIFTiPhoneCaptureCore/
UMIFTiPhoneCaptureCore.xcodeproj
```

当前实现使用 ARKit 主相机流，并通过 runtime private getter 获取超广角副流。该私有接口适用于内部采集工具，不适合作为 App Store API 依赖。构建和真机部署见该目录的 `README.md`。

### 5.3 CoinFT + Teensy

固件、采集脚本和标定资产位于：

```text
modules/03_coinft_teensy/steps/
modules/03_coinft_teensy/assets/calibration/production/
modules/03_coinft_teensy/configs/coinft_04_laptop_calibrated.json
```

左右传感器的权威映射见：

```text
modules/03_coinft_teensy/assets/calibration/production/current_left_right_mapping.json
```

选择 ONNX 模型时，模型外部数据文件必须与 `.onnx` 保持在同一目录。

### 5.4 Mac 主 GUI、对齐与导出

```bash
cd modules/04_laptop_alignment_export

python3 entrypoints/receiver_web_gui.py \
  --host 127.0.0.1 \
  --port 8767 \
  --no-open \
  --no-auto-start
```

浏览器打开 `http://127.0.0.1:8767/`。完整首次部署、D435 sudoers、iPhone 安装、Dataset/Episode 操作流程见：

```text
modules/04_laptop_alignment_export/docs/01_operator_guide.md
```

单个 Episode 的 UMI-FT replay buffer Zarr 导出示例：

```bash
python3 entrypoints/export_run.py <run_dir> <source_episode_index> \
  --format umift-replay-buffer-zarr \
  --config config/export_profiles/umift_replay_buffer_v0.json
```

## 6. CoinFT 模型选择

GUI 不应依赖硬编码模型路径。启动 CoinFT 前，在 GUI 的 Controls And Details 中设置左右 ONNX/norm 路径。包内当前 production 模型包括传感器 `coinft_2606602_0019`、`coinft_2606602_0012` 和 `coinft_2606602_0008`；请以 `current_left_right_mapping.json` 和现场设备编号为准，不能仅凭旧文档假定左右关系。

## 7. 开发自检

```bash
cd modules/04_laptop_alignment_export

python3 -m py_compile entrypoints/receiver_web_gui.py
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. \
  python3 -m pytest -q -p no:cacheprovider
```

涉及 D435、iPhone、Teensy 或 CoinFT 的完整验收需要连接真实设备；仅运行单元测试不能替代硬件验收。

本次封包前已使用原开发机的采集环境完成主 GUI 入口语法检查。该环境当时未安装 `pytest`，因此包内 15 个 pytest 测试文件未在封包步骤中执行；接收方安装 `pytest` 后可用上面的命令复验。

## 8. 数据和操作注意事项

- `runs/<run_id>/raw/` 是原始数据，不要手工改写。
- 不要用 Finder 单独改名、合并或删除 Dataset 内的 Episode，否则会破坏 `RUN_MANIFEST.json` 的索引和导出映射。
- iPhone 录制按钮负责开始/结束 Episode；浏览器 `Stop` 用于停止整套设备，二者不能混用。
- 串口 `/dev/cu.usbmodem...`、iPhone UDID、Xcode 路径、RealSense serial 都可能随机器或设备变化。
- 首次安装 D435 sudoers 会修改系统配置并要求管理员密码，请按操作指南核对脚本后执行。

## 9. 完整性校验

压缩包同目录提供 `.sha256` 文件。校验命令：

```bash
shasum -a 256 -c UMIFT_datacollect_source_20260812.zip.sha256
```

包内 `FILE_MANIFEST.sha256` 用于校验解压后的逐文件内容：

```bash
cd UMIFT_datacollect_source_20260812
shasum -a 256 -c FILE_MANIFEST.sha256
```
